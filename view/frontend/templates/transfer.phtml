<?php
/** @var \Punchout2Go\HyvaCompat\Block\Config $block */
/** @var \Punchout2Go\Punchout\Helper\Transfer $helper */

$helper = $this->helper(\Punchout2Go\Punchout\Helper\Transfer::class);
$isDebug = (bool) $helper->getIsDebug();
?>

<div
    class="punchout-transfer-button"
    x-data="p2gTransferButton({
        debug: <?= $isDebug ? 'true' : 'false' ?>
    })"
>
    <button
        type="button"
        class="bg-blue-600 hover:bg-blue-700 text-white py-3 px-6 rounded-lg font-semibold disabled:opacity-50 disabled:cursor-not-allowed"
        :disabled="loading"
        @click.prevent="handleClick()"
    >
        <span x-show="!loading">
            <?= $escaper->escapeHtml($helper->getButtonLabel()) ?>
        </span>

        <span x-show="loading">
            Transferring…
        </span>
    </button>

    <?php if ($help = $helper->getButtonHelp()): ?>
        <p class="mt-2 text-sm text-gray-500">
            <?= $escaper->escapeHtml($help) ?>
        </p>
    <?php endif; ?>

    <!-- Debug hint -->
    <div
        class="text-xs mt-1 opacity-70"
        id="p2g-debug-hint"
        x-show="debug"
        x-text="hint"
    ></div>
</div>